doctype html
html
  head
    meta(charset="utf-8")
    title= title
    link(rel="stylesheet", href="/public/css/style.css")
  body
    main.container
      h1 Integrating With HubSpot I Practicum – HubSpot Pets
      p
        a.button(href="/update-cobj") Add to this table

      if rows && rows.length
        table.data-table
          thead
            tr
              th Name
              th Animal
              th Birth country
          tbody
            each row in rows
              - var props = (row && row.properties) ? row.properties : {};
              tr
                td= props.name || ''
                td= props.animal || ''
                td= props.birth_country || ''
      else
        p No records found yet. Add one!
